[project]
name = "ai-bootcamp"
version = "0.1.0"
description = "AI Engineering Bootcamp — учебный курс с примерами кода"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "markdown>=3.10.2",
    "pygments>=2.19.2",
    "weasyprint>=63.1",
]

[dependency-groups]
dev = [
    "ruff>=0.15.7",
]
